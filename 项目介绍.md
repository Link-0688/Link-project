# 项目简介

一个跑在STM32F407VET6上的微型桌面系统，用FreeRTOS做任务调度
屏幕是 128*64 的 OLED，走软件SPI数据都存在 SD 卡上；输入用旋转编码器 + 板载按键
编译环境是 VSCode + CMake没用Keil5，因为 FreeRTOS 编译出来超过32K，免费版放不下

## 一.四个界面

### （1）：开始界面
长按 KEY_2 2秒后进入密码输入界面。顶部显示检测线状态，中间显示时间，底部为提示
检测线（PC4）接GND：绿灯为呼吸灯，DEVICE:NORMAL
没有接GND：红灯以1秒为周期闪烁，DEVICE:ERROR

### （2）：密码界面
点击 KEY_1/2/3/4 分别代表数字1.2.3.4，输入满四位自动与正确密码比较
密码正确：进入主菜单界面
密码错误：显示ERROR，密码清空，重新输入；连续输错3次进入锁定界面5秒

### （3）：主菜单界面
左上角显示系统时间，中间为6个功能，左右旋转编码器选择要进入的功能。
点击编码器按钮进入功能界面，再次点击按钮返回主菜单界面

### （4）：功能界面

**在任意界面长按KEY_4 2秒后进入熄屏状态，长按KEY_1 2秒后唤醒屏幕**

## 二.七大功能

### 【1】文件管理 具有查看，删除，新建，重名检测功能
左右旋转编码器选中想要操作的文件，点击KEY_3可查看文件详情。
点击KEY_4弹出删除界面，KEY_1表示删除并刷新列表，KEY_2表示取消操作
点击KEY_1弹出新建界面，再次点击KEY_1表示新建文件，KEY_2表示取消操作
最多可新建16个文件，删除之后再新建会优先填上数字较小的空位

**若文件个数为0 界面会显示EMPTY**
*由于性能有限，如果多次快速新建文件，文件会新建成功，但在OLED屏幕会显示卡屏*

### 【2】画图 具有上下左右移动，落笔，一键清除，存储功能
上下由按键KEY_1和KEY_2控制，左右由编码器左右旋转控制，KEY_3为落笔，KEY_4为删除
画图数据保存在SD卡不会丢失，每次进入界面都会显示上次画过的数据
长按KEY_3 2秒后进入一键清除功能，点击KEY_1清空画布，KEY_2取消操作

### 【3】音乐播放 具有播放，暂停，切歌功能
左右旋转编码器选择歌曲，点击KEY_3播放音乐，点击KEY_4暂停音乐，OLED最下方显示音乐播放状态

### 【4】日志记录 具有记录、存储功能
日志会自动记录密码输入时间，熄屏操作时间，点击每一个功能的时间（以系统时间为准）

### 【5】监控
第一页：顶部显示UP（运行秒数）和Task（任务数），下方显示各个功能的名称和状态
第二页：显示CPU,IDLE等各个状态占用率
第三页：健康监测，显示上次异常复位的原因和异常复位的次数

点击KEY_3实现页面切换

### 【6】系统设置 具有调节光标灵敏度，大小，熄屏时长，屏幕亮度，音乐音量功能
左右旋转编码器选择需要调整参数的设置，点击KEY_3进入编辑模式
此时左右旋转编码器就是调整参数，再次点击KEY_3保存参数设置

### 【7】串口CLI 输入特定指令可以查看每个功能运行状态

## 三.其它系统特性

### 存储一致性
CONFIG.BIN（系统设置）和DRAW.PIC（画图数据）这两个文件加了魔数头加CRC32校验
写的时候先写一个临时文件，全部写完再改名替换掉原来的文件，防止写一半掉电把原来的数据弄没了
读的时候先看魔数对不对，再自己算一遍CRC跟存的比对，有一个不对就当成损坏，用默认值

### 看门狗
加了看门狗（IWDG），超时大概4秒，健康任务每1秒喂一次狗
程序要是卡死没人喂狗，4秒后自动复位重启

### 健康监测与崩溃转储
系统盯着输入、系统、界面这三个任务的心跳，超过5秒没心跳就当成卡死了
卡死或者触发HardFault的时候，把原因写进SD卡的CRASH.LOG，同时用RTC的后备寄存器记下复位次数和原因，复位之后在监控第三页能看到

### RGB呼吸灯
绿灯做呼吸效果。因为PD13/PD14跟蜂鸣器共用TIM4，蜂鸣器一改ARR就会带乱呼吸灯，所以呼吸灯改用软件PWM（1ms的软件定时器）来做

### 中断输入
按键和编码器按键从原来的轮询改成了EXTI中断加软件定时器消抖，编码器旋转还是用TIM2硬件计数

### 运行负载分析
监控第二页显示各个任务的CPU占用率，时基用TIM5，占用率 = 100-空闲任务占比
