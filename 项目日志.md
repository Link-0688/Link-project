# 项目日志

## 第一阶段 环境与驱动（8.17-8.19）

### 8.17 12点 搭建环境、建git仓库、移植驱动库代码
内容：将蜂鸣器，串口助手，OLED屏幕代码移植到项目中并在FreeRTOS中加入调试代码，OLED成功亮起来

### 8.17 18点 加LED和按键模块
内容：加入LED灯，按键模块，实现由按键控制LED灯亮灭，加入编码器、SD卡底层模块
备注：编码器、SD卡模块只是移植驱动库代码，还没进行调试

### 8.19 12点 编码器、串口输出、按键消抖与长按
内容：加入旋转编码器、串口助手输出功能，外加所有按键的消抖，板载按键长按识别功能
卡点：1.板载按键识别功能异常，没有达到2秒输出KEY LONG的效果
2.旋转编码器一直没反应，串口助手一直输出UNPLUG，无论检测线是否接通
3.串口助手输出与实际按键不对应，串口助手输出为0123，实际应为1234
解决方案：1.逻辑错误 if(p_k->hole_ms += LONG_PRESS_MS && !p_k->long_sent)   //+=应为>=

### 8.19 20点 解决编码器左右旋转无输出
内容：解决旋转编码器左右旋转无输出
病因：编码器硬件写的是5V,所以一直接5V，但是PA0/PA1/PA11不适用
解决方案：改为3.3V，检测线和按钮能够正常反应，但左右旋转还不行
左右旋转是因为硬件上拉电阻不够，PA0/PA1被悬空，需要手动把PA0/PA1从NOPULL改为PULL-UP

## 第二阶段 界面骨架（8.20）

### 8.20 16点 完成UI界面渲染
内容：完成UI界面渲染包括起始，登录，主菜单界面
卡点：1.RTC时钟没有计数而是静止不动 2.屏幕显示出现重影
病因：RTC读取需要先Time后Date,不能只读取Time
oled_show_string没有合理分配位置
解决方案：改写RTC读取函数，逐个调试显示函数的x,y参数

## 第三阶段 六大应用（8.22-8.26）

### 8.22 12点 SD卡功能页面基础功能
内容：实现SD卡功能页面的基础功能(支持删除，查看)
卡点：1.烧录进去后卡在SPI-while死循环里，OLED屏幕不亮
2.SD卡读取失败，地址返回0x00
3.出现bug1:旋转编码器逆时针旋转失灵，顺时针正常
4.出现bug2：旋转编码器按钮点击可以进入功能页面，再次点击无法返回主菜单页面
病因：1.SPI初始化需要放在dev_oled_init()前面
2.先检查软件方面：SD卡SPI必须为模式0，之前调为模式3
然后检查硬件方面：本来接5V，一直为0x00；后来接3.3V显示Timeout
原因是供电不足，SD卡上有AMS1117 仅靠ST-LINK和USB转TTL不足以供电 需要外接Type-C才能满足需求
3.Cubemx配置没有办法调TIM2的上拉电阻，每次重新配置完之后都需要在tim.c调为上拉电阻
4.缺少dirty = 1界面转换标志，在OLED屏幕没有显示

### 8.22 22点 修复字体丢失和功能丢失
内容：修复部分字体丢失和功能丢失问题
卡点：1.OLED屏幕部分界面最下方字体未能正常显示，文件删除功能异常
解决方案：1.oled(x为128,y为64，采用8*16字模)则x可显示16个字母，y可显示4个字母
要想显示最多，则必须从0开始，按照倍数依次填写x,y参数
2.删除功能异常原因为按键名称错误，改为EVT_KEY_PRESS

### 8.23 12点 完成基础画图功能
内容：完成基础画图功能(按键+编码器实现单点像素标注)
后续优化：支持按键长按能一直画点
卡点：像素落点操作算法较难理解

### 8.24 12点 完成音乐播放功能
内容：完成音乐播放功能(支持暂停，切歌)
卡点：OLED屏幕显示逻辑正常，但是蜂鸣器不响
病因：面包板接线不良且无源蜂鸣器需接5V电源

### 8.25 12点 完成日志记录功能
内容：完成日志记录功能(根据时间戳记录操作)
卡点：引发BUG(密码输入卡在最后一位，没有办法输入)
病因：问题在于在process_login函数中给了互斥锁，但是函数里面又调用了APP_Log_Add函数，
该函数里面又使用了一次互斥锁，导致重复拿一把锁，使TaskSys卡死
解决方案：在login函数中采用局部变量log_msg暂存结果
把 APP_Log_Add 挪到 APP_UIModel_Unlock()之后再调用

### 8.25 20点 完成监控功能
内容：完成监控功能(显示运行时长、任务数)
卡点：滚动超过真实行数后，没有任何行落在可见区
病因：s_scroll < 12超过任务数上限
解决方案：新增s_max_scroll变量，计算任务总数，防止越界

### 8.26 12点 完成系统设置功能
内容：完成系统设置功能(调节光标灵敏度，大小，熄屏时长，屏幕亮度)
卡点：1.设置完之后点击旋转编码器按钮出现黑屏
2.亮度调整并没有发生变化
3.光标大小出现越界情况
4.熄屏时长永远为30秒
病因：1.系统设置默认各项参数初始值为0，导致设置一旦生效，直接黑屏和无限制熄屏
2.代码逻辑问题
3.光标大小默认为8才越界，但实际范围应为1~4
解决方案：1.修改各项参数，不能全为0
2.给BRIGHTNESS加上赋值语句
3.修改光标大小宏定义
4.改app_sys.c中熄屏时长参数，把固定30改为DEV_Config_Get().sleep_time

### 8.26 22点 全项目检查、修复前三阶段隐含BUG
内容：系统检查整个项目，修复前三阶段隐含的BUG
卡点：1.日志功能每次都只显示最新的一条，并没有显示开机以来的所有操作
2.OLED偶尔花屏，长时间运行会卡死
修复内容：
1.删除了dev_music中原来存在的小星星音乐
2.删除了dev_spi_oled.c中没有调用过的dev_oled_write_data
3.软件 SPI 跨任务竞态（TaskUI 刷新 vs TaskSys 熄屏/对比度命令抢 CS 时序）
给两个任务加上互斥锁
4.open_app()每次都调用了APP_Log_Init()，导致缓冲区数据都被清除掉，所以才只显示最新一行操作
APP_Log_Init() 移到 TaskSys 上电时执行一次
进入 LOG 界面改为调用新增的 APP_Log_Open()，只把滚动复位到最新一条，不清空历史

## 第四阶段 系统优化（8.27-8.29）

### 8.27 16点 重写app_monitor.c，增加运行负载分析
内容：重写app_monitor.c，增加功能运行负载分析，在MON页面新增CPU占用率
卡点：编码器逻辑出现问题，其他页面旋转正常，但在MON页面旋转一次则卡死
备注：由于Cubemx不能将PA0/PA1调为上拉电阻，所以每次配置为Cubemx都需要重新在tim.c修改为上拉
GPIO_InitStruct.Pull = GPIO_PULLUP;
解决方案：补上了向左回转的逻辑同时加上脏刷新标志

### 8.27 22点 重写app_input.c，按键改中断触发
内容：重写app_input.c，将轮询按键改为中断触发+软件定时器
备注：同理Cubemx重新配置时改变了FREERTOS环境，需要手动调代码
#define configUSE_TRACE_FACILITY                 1
#define configUSE_STATS_FORMATTING_FUNCTIONS     1
#define configGENERATE_RUN_TIME_STATS            1

### 8.28 12点 OLED改按需刷新+页级局部刷新
内容：改动app_ui.c和dev_oled.c，将100ms定时刷新改为按需刷新+页级局部刷新

### 8.28 23点 完善日志清空功能
内容：改动app_log.c，完善日志清空功能，并显示记录了多少条日志

### 8.29 20点 补全音量调节、画图清空、新建文件
内容：补全音量调节，画图清空，新建文件功能
卡点：系统设置界面没有产生滚动效果且刷新的时候会卡顿
病因：代码写错了功能没有生效，没有加脏标志
解决方案：所有触发刷屏的动作都加上脏标志

## 第五阶段 进阶与收尾（8.30-9.3）

### 8.30 10点 存储一致性（CRC32+魔数头）
内容：采用CRC32校验+魔数头实现存储一致性
CONFIG.BIN 和 DRAW.PIC 都走这套：文件头是[魔数4字节][CRC4字节][数据]，读和写都过一遍
验证方法：
1.正常走一遍：改设置保存，断电重新上电，设置还在，说明读写链路正常
2.把卡插电脑上，用十六进制工具改 CONFIG.BIN 第一个字节（魔数），装回去上电，设置回到默认值，说明魔数校验生效
3.只改数据部分不改魔数，还是会回到默认值，说明CRC校验生效
现象：校验不过的时候是“回到默认值”，不会花屏也不会出现乱值，就是宁可用默认值也不用坏数据

### 8.31 20点 任务异常检测+崩溃转储
内容：做任务异常检测+崩溃转储
盯着输入、系统、界面三个任务的心跳，超过5秒没心跳就当成卡死
卡死或者HardFault就写一条 CRASH.LOG 到SD卡，同时用RTC后备寄存器记下复位次数和原因，监控第三页显示这些信息
验证方法：
1.串口输入 crash，会打印 [CRASH] HARDFAULT 然后复位，重启后监控第三页 RST 加1、原因显示 HARDFAULT，SD卡根目录多出 CRASH.LOG
2.串口输入 stuck Input（或者 sys/ui），大概5秒后打印 [CRASH] INPUT STUCK 然后复位，重启后原因显示 INPUT STUCK
3.上面两步做完，正常断个电再上电，RST 次数不变，说明后备寄存器是靠VBAT维持的
现象：刚上电监控第三页显示 RST:0 UNKNOWN 是正常的，说明后备寄存器刚被初始化

### 8.31 22点 设置看门狗
内容：设置看门狗
用IWDG，超时大概4秒，健康任务每1秒喂一次狗
验证方法：
1.正常跑：长时间不停机不重启，说明喂狗周期正常
2.串口输入 stuck Health 把喂狗的任务挂起，4秒后板子会自己重启，串口重新打印 [CLI]ready
现象：看门狗复位不走崩溃转储那套，所以健康页的RST次数不会加1、原因也不变，可以拿这个区分是看门狗复位还是崩溃复位

### 8.31 23点 修监控页任务位置乱跳
内容：MON界面第1、2页health在每次编码器滚动后会乱跳
病因：任务列表原来是按“就绪/阻塞”分区显示的，Health任务每秒只运行一瞬间，其余时间都是阻塞，位置就在最上面和最下面之间来回跳
解决方案：改用 uxTaskGetSystemState 拿到任务后按固定优先级降序排序，不再按状态分区；健康那一页也不响应编码器滚动
验证现象：多进几次监控页，任务位置固定不动

### 9.1 22点 增加串口CLI输出
内容：增加串口CLI输出
卡点：串口助手能发命令给板子，但板子收不到发过去的字符
病因：上电的时候 UART 有残留数据触发了 ORE/FE 错误，接收中断就停了
解决方案：补一个 HAL_UART_ErrorCallback，清掉 ORE/FE 标志再把接收重新开起来
验证现象：串口输入字符板子能收到，能正常解析命令

### 9.2 22点 增加LED灯不同状态下显示逻辑
内容：增加LED灯不同状态下显示逻辑
卡点：呼吸灯升到最亮的时候感觉不够亮，只有大概四分之一
病因：亮度公式除法顺序写反了，上升段最高只有24%
解决方案：改 dev_status_led.c 里的除法顺序
验证现象：呼吸灯能升到全亮

### 9.3 清理遗留问题、修烧录后现象
内容：清理前面几个阶段遗留的小问题，顺便修烧录之后发现的现象
卡点：1.SD卡读写很慢 2.退出应用要等1秒才回到桌面 3.设置界面滚动到末项回绕的时候选中项会跑出屏幕
病因：1.SD卡初始化完之后 SPI2 一直停在164kHz没升速
2.退出的时候是先同步写SD再切界面，卡在写卡上了
3.编码器从末项回绕到首项的时候 s_scroll 没跟着跟，选中项滚出可见区
解决方案：1.初始化完成后调 SD_EnableFastSpi() 把 SPI2 切到10.5MHz
2.退出改成先把状态和dirty置好让界面先响应，再存SD
3.加一个 scroll_to_item() 让 s_scroll 跟着选中项走
验证现象：SD落盘明显变快；退出一点就回桌面；设置滚到底再往下能正常回绕
备注：红灯常亮最后查出来是硬件问题——检测线 PC4 悬空等于断开，所以进了红灯ERROR模式，不是代码的问题

## 验收准备（9.4）

### 9.4 开始准备线下验收
内容：代码收尾完，开始准备线下验收
自己做了一份自问自答的文档，挑自己不会的题每天弄懂4-5道，又把项目日志和项目介绍这两份文档整理了一遍
卡点：批改的时候查出来几个硬错
病因：位运算的置0和取反写反了（置0应该是 data &= ~(1<<n)，写成 data &= (1<<n) 会把别的位清零）；低转高没区分有没有符号，有符号的要补符号位（int8_t 的 -1 转 int16_t 是 0xFFFF，不是 0x00FF）
解决方案：把这两个点单独拎出来重新背一遍
