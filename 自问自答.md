# 1.说明开漏输出的特点、原理，以及I2C为什么需要开漏输出？
*开漏输出原理：它没有上端的P-MOS管，只有接地的N-MOS管，当输出为0时，它会置为低电平，输出为1时，如果有外部上拉电阻，会输出高电平*
*特点：不能主动地输出高电平*
**作用**
1.能够进行电平转换：STM32默认是3.3V电源，如果外部接上能够支持5V的上拉电阻，可以将电压安全转化为5V
2.满足“线与逻辑”：当所有外设都接到电源(为高电平)时才会把总线拉高（高阻态），只要有一个还处于低电平，总线就为低电平

*I2C需要开漏输出的原因：I2C会有多个硬件外设挂载在同一条总线上，如果采用普通的推挽输出，会导致有的外设为高电平，有的为低电平，*
*这样一来就会形成通路，结果把总线短路了，会烧坏芯片。*
而开漏输出满足线与逻辑，能够保证芯片不被烧坏，不会有过大电流流入芯片。

# 2.裸机开发和FREERTOS的本质区别
*1.从耦合度来看*
裸机主要都集中在main.c中的while循环来执行逻辑，耦合度很高，很难支撑大型项目跑逻辑；
FREERTOS通过任务列表和队列机制，实现一个功能对应一个任务，做到了“高内聚、低耦合”
*2.从运行逻辑来看*
裸机为上下文逻辑，一旦进入一个函数，就必须从头跑到尾，除了硬件中断能够打断；
FREERTOS通过优先级划分和每个任务都有独立的栈空间，可以实现在任务执行到一半时，跳出去执行另外其他任务，再回来继续执行原任务；
这极大增强了实时性和运行效率
*3.从CPU运行效率来看*
裸机在被阻塞等待的时候，是不断运行while()死循环，CPU算力被100%占用，没有办法执行其他逻辑，这极大浪费了算力；
FREERTOS在被阻塞等待的时候，会跳出去执行其他处在就绪状态的任务，如果没有就绪态也会进入空闲状态，减少CPU消耗；

# 3.边沿和电平的区别、上升/下降沿的区别以及应用场景
边沿：是指电平由低变高/由高变低的那一瞬间，强调的是瞬时状态
电平：是指某一时间段的状态，强调的是状态区间

假设按键按下时电平变化是由高变低
上升沿：是指电平由低变高的状态，即按键松手时刻
下降沿：是指电平由高变低的状态，即按键按下时刻
应用场景：根据需求判断是要检测按下瞬间还是松开瞬间执行逻辑，如果要实现长按功能，则需要双边沿检测

**边沿与电平的选择**
以旋转编码器为例，其原理为左右旋转会产生方波，进而产生电平变化
A相产生边沿变化时，会去检测B相的电平，从而判断是自增还是自减
所以对于A相要采用边沿检测，对B相要采用电平检测

# 4.手写出裸机消抖函数以及FREERTOS消抖
**裸机阻塞式消抖**
#include "main.h"

#define KEY_PORT    GPIOB
#define KEY_PIN     GPIO_PIN_5

uint8_t Key_Scan_Blocking(void)
{
    if(HAL_GPIO_ReadPin(KEY_PORT,KEY_PIN) == GPIO_PIN_RESET)
    {
        HAL_Delay(20);
        if(HAL_GPIO_ReadPin(KEY_PORT,KEY_PIN) == GPIO_PIN_RESET)
        {
            while(HAL_GPIO_ReadPin(KEY_PORT,KEY_PIN) == GPIO_PIN_RESET);
            return 1;
        }
    }
    return 0;
}

**裸机非阻塞式消抖**
#include "main.h"
#include <stdint.h>

#define KEY_PORT    GPIOB
#define KEY_PIN     GPIO_PIN_5
#define DEBOUNCE_MS     20

typedef enum{
    KEY_IDLE = 0,
    KEY_DEBOUNCE,
    KEY_PRESSED
}key_state_t;

uint8_t Key_Scan_NonBlocking(void)
{
    static key_state_t s_state = 0;
    static uint8_t key_pressed = 0;
    static uint32_t count = 0;
    switch(s_state)
    {
        case KEY_IDLE : if(GPIO_ReadPin(KEY_PORT,KEY_PIN) == GPIO_PIN_RESET)
        {
            count = HAL_GetTick();
            s_state = KEY_DEBOUNCE;
            break;
        }
        case KEY_DEBOUNCE : if(HAL_GetTick() - count >= DEBOUNCE_MS)
        {
            if(GPIO_ReadPin(KEY_PORT,KEY_PIN) == GPIO_PIN_RESET)
            {
                key_pressed = 1;
                s_state = KEY_PRESSED;
            }
            else
            {
                s_state = KEY_IDLE;
            }
        }
        break;
        case KEY_PRESSED : if(HAL_GPIO_ReadPin(KEY_PORT,KEY_PIN) == GPIO_PIN_SET)
        {
            s_state = KEY_IDLE;
        }
        break;
        default : break;
    }
    return key_pressed;
}

**FREERTOS非阻塞消抖**
#include "main.h"
#include "task.h"
#include "FreeRTOS.h"

#define KEY_PORT    GPIOB
#define KEY_PIN     GPIO_PIN_5
#define DEBOUNCE_TARGET  2

uint8_t Task_KeyScan(void *pvParameters)
{
    static uint8_t count = 0;
    static uint8_t key_pressed = 0;
    for(;;)
    {
        if(GPIO_ReadPin(KEY_PORT,KEY_PIN) == GPIO_PIN_RESET)
    {
        if(count < DEBOUNCE_TARGET) count++;
        else if(key_pressed == 0)
        {
            key_pressed = 1; 
            xQueueSend(xKeyQueue,&key_id,0);
        }
    }
        else
        {
            count = 0;
            key_pressed = 0;
        }
    
    vTaskDelay(pdMS_TO_TICKS(10));
    }
}
# 5.软件/硬件定时器的应用场景、区别，定时器作用，内部/外部时钟的区别
**定时器本质：根据时钟基准进行自增自减的计数器**
**定时器作用**
*1.作为时钟基准*
*2.稳定输出PWM波形（cnt与CCR比较，决定占空比）*
*3.输入捕获和测量*
*4.旋转编码器解码*

**软件定时器与硬件定时器区别**
*1.从接线角度看*
硬件定时器强制要求外设引脚要接到特定的定时器引脚上，软件定时没有要求
*2.从时间精度看*
硬件定时器精度为1us（适用于PWM输出波形、超声波测距），软件定时器精度为1ms（适用于LED亮灭、OLED熄屏）
*3.从数量上看*
硬件定时器只有几个到十几个，数量有限。软件定时器理论上只要有足够的栈空间分配，可以有无数个。
*4.软件定时器缺陷*
裸机开发中如果将阻塞延时和软件定时器写在同一个函数里，软件定时器超时响应会被严重推迟，造成时序误差
FREERTOS中需要给软件定时器足够高的优先级，使它能够抢占低优先级任务立即执行

**内部时钟与外部时钟的区别**
内部时钟依靠APB1/APB2总线分频得到基准频率，以计时为主；
外部时钟依靠外部事件触发获得，以计数为主；

**软件定时器代码**（采用时间戳）
uint32_t last_time = HAL_GetTick();
while(1)
{
    if(HAL_GetTick() - last_time >= 100)
    {
        last_time = HAL_GetTick();
        //执行其他任务
    }
}

# 6.回调函数含义、常见类型及注意事项
普通函数的逻辑是上层主动调用下层函数，而回调函数是You dont call me,i will call you，在特定事件触发后会调用上层函数
*常见类型*
**1.硬件中断回调函数**
（1）串口中断回调函数
（2）按键中断回调函数
（3）定时器中断回调函数
**2.软件定时器中断回调函数**
**3.外部引脚中断回调函数**
*注意事项*
1.快进快出，严禁阻塞
2.检查收尾工作（串口需要Receive_IT、FreeRTOS中需要YIELD_FROM_ISR(vTaskPriorityTaskWoken)
3.不能使用带阻塞的API，要使用FromISR

**补充：钩子函数含义及用法、为什么不能都用FromISR**
*钩子函数含义：本质上是事件回调函数、在系统发生重大级错误的时候会自动调用*
常见场景有：滴答定时器每触发一次调用一次、动态内存没有足够空间分配的时候、魔数被篡改的时候、无就绪任务进入空闲状态时
*为什么FromISR只能且必须在中断里使用*
FromISR严禁一切阻塞延时，如果在正常逻辑里调用，存在几个问题
1.系统需要阻塞给CPU让位     当一个任务执行完时会进入阻塞态，CPU会切换到执行其他任务，但如果没有延时，CPU无法切换，只能跑while死循环，变成跟裸机一样
2.生产者和消费者的矛盾  即流量反压 生产者和消费者的耗时不一样，需要延时先把生产者的队列填满再给消费者，这样能使程序效率提高
3.破坏了互斥锁的特性    互斥锁没有中断版本，互斥锁具有优先级继承和任务所有权绑定的特性，但是中断不属于任何任务
4.破坏了上下文逻辑  FromISR不能被随意打断，不能像普通函数一样执行到一半切换至其他任务

# 7.什么是句柄结构体、用法是什么？
结构体本质就是将一堆需要定义的变量整合在一起，把他们分组归类，做到“高内聚、低耦合”的思想
结构体可以分为三种类型：自定义结构体、HAL库结构体、FreeRTOS结构体
在自定义结构体的时候，需要考虑几个因素
**1.结构体的位置与区域**
放在.c是为了让函数内部调用，通常为一些静态变量参数（配置数据），放在.h是为了让其他任务调用，通常为一些状态常量（硬件配置）
区域是放在SRAM还是Flash（用const关键字修饰）
**2.结构体对齐原则**
对结构体变量命名的时候需要注意uint8_t/uint32_t尽量放在一起，减少结构体占用空间
**3.结构体的内容**
什么变量应该放在里面，什么不应该，这取决于变量与实现功能的相关度
**4.结构体的保护**
例如结构体需要被多个任务调用，那就要考虑是否要加互斥锁来保护变量
结构体变量在中断里更新，那就要考虑是否要加volatile关键字确保数据读取正确

**HAL库结构体与FreeRTOS结构体区别**
HAL库结构体通常以h开头命名变量，结构体名称一般以HandleTypeDef结尾；它的调用是透明的，形式一般为huart->Instance
FreeRTOS结构体通常以x开头命名变量，结构体名称一般以Handle_t，TaskHandle_t结尾；它的调用是不透明的，形式一般为xQueueSend(Queue,...)

# 8.PWM相关知识点（ARR/PSC/CCR）参数设置考虑因素
PSC(预分配器)：将APB1/APB2总线的频率进行分频，决定数一步需要多久时间
ARR(自动重装载寄存器)：将频率进行切割划分，决定精度大小，CNT从0计数到ARR，接着开始新一轮循环
CCR(输入捕获寄存器)：决定占空比在PWM1模式下，CNT < CCR时输出高电平，CNT > CCR时输出低电平
f = 定时器频率 / (PSC + 1) / (ARR + 1)
Duty(占空比) = CCR / (ARR + 1)
PSC和ARR参数考虑因素
1.PSC决定数一步要多长时间，一般都设为83，将时钟基准定为1us
2.ARR根据实际功能进行调整，例如LED呼吸灯可以设为99，0~100精度刚好对应百分比，也不需要太高的精度
蜂鸣器播放音乐就需要设为999，划分0~1000精度，便于实现不同音调
3.在频率确定的情况下，尽可能调小PSC，调大ARR，获得更高的分辨率

# 9.DMA+IDLE功能、原理、使用场景
DMA(Direct Memory Access):直接内存访问
IDLE:空闲中断
DMA+IDLE功能：可以接收不定长字符串，接收到的字符串接收完后开启中断打断CPU，将数据搬运进CPU中
DMA机制：需要先给一个固定大小的数组接纳字符，自减机制：要计算接收到多少字符需要数组大小-CNT计数值；
**三种通信模式选择**
阻塞 Polling  状态  Status

*1.阻塞式通信(轮询)：CPU反复确认是否有字节发送过来，直到收到指定长度的字节或者超时退出*
适用于代码调试时用UART检查现象是否正确

#include "main.h"
#define RX_LEN  10
uint8_t g_rx_buf[RX_LEN];

void UART_Receive_Blocking(void)
{
    HAL_StatusTypeDef status = HAL_UART_Receive(&huart1,g_rx_buf,RX_LEN,1000);
    if(status == HAL_OK)
    {
        //成功接收10字节，处理业务数据
    }
    else if(status == HAL_TIMEOUT)
    {
        //超时未收齐，处理超时异常
    }
}

*2.非阻塞式通信(中断):每接收到一个字节，就打断一次CPU，在中断回调中保存数据*
适用于按键触发类命令交互(CLI) 例如敲字符‘1’和‘2’控制LED灯亮灭

#include "main.h"
uint8_t g_rx_byte;

void UART_Receive_IT_Start(void)
{
    HAL_Receive_IT(&huart1,&g_rx_byte,1);
}

//重写弱函数
void HAL_UART_RxCpltCallBack(UART_HandleTypeDef *huart)
{
    if(huart->Instance == USART1)
    {
        //处理收到的单字节数据
        //重点：非循环中断必须要手动重启下一次字节接收！
        HAL_UART_Receive_IT(&huart1,&g_rx_byte,1);
    }
}

*3.DMA+IDLE(直接内存访问配合串口空闲中断)*
接收不定长数据，RTOS核心通信通道

#include "main.h"
#include "FreeRTOS.h"
#include "queue.h"

#define RX_BUFFER_SIZE  128

uint8_t g_dma_rx_buf[RX_BUFFER_SIZE];   //给出定长数组
extern QueueHandle_t xQueueUart;    //业务队列句柄

void UART_DMA_IDLE_Start(void)
{
    __HAL_UART_ENABLE_IT(&huart1,UART_IT_IDLE);
    HAL_UART_Receive_DMA(&huart1,g_dma_rx_buf,RX_BUFFER_SIZE); //启动DMA接收
}

void USART1_IRQHandler(void)
{
    /*检查是否发生空闲中断*/
    if(__HAL_UART_GET_FLAG(&huart1,UART_FLAG_IDLE) != RESET)
    {
        // 1.清除空闲中断标志位 
        __HAL_UART_CLEAR_IDLEFLAG(&huart1);
        // 2.暂停DMA接收 
        HAL_UART_DMAStop(&huart1);
        // 3.计算有效长度
        uint16_t rx_len = RX_BUFFER_SIZE - __HAL_DMA_GET_COUNTER(huart1.hdmarx);
        // 4.将数据包发送至FreeRTOS队列或者通知任务处理
        BaseType_t xHigherPriorityTaskWoken = pdFALSE;
        if(rx_len > 0)
        {
            xQueueSendFromISR(xQueueUart,g_dma_rx_buf,&xHigherPriorityTaskWoken);
        }
        // 5.重新配置并重启下一次DMA接收
        HAL_UART_Receive_DMA(&huart1,g_dma_rx_buf,RX_BUFFER_SIZE);
        // 6.触发上下文切换
        portYIELD_FROM_ISR(xHigherPriorityTaskWoken);
    }
    HAL_UART_IRQHandler(&huart1);
}
# 10.volatile.const关键字作用
volatile(易变的)：为了提高运行效率，通常程序运行会把变量从物理寄存区丢入CPU高速暂存寄存器内，每次读取值默认从寄存器内读取，
但是因为函数内没有改变变量的值，而在中断或者其他位置改掉了，函数依旧会读取在寄存器的值，这时就需要加volatile让系统强制从物理寄存区内读取最新的值
const(只读属性)：将常量数组和大型变量放进Flash区域，不占用宝贵的SRAM内存，比如OLED字符数组，蜂鸣器的乐谱

**纠错：全局/静态的 const 变量才进 .rodata 段（STM32 链接到 Flash）；局部 const变量仍在栈（SRAM）上**

# 11.什么是挂载、通信、线与逻辑？
挂载(mount):挂载分为两个层面，一个是物理硬件层面，一个是软件层面
硬件层面：多个外设的通信引脚接在同一个引脚上，这样可以节省引脚资源
软件层面：以SD卡举例，SD卡插入读卡器中，只是电气引脚接通，并不代表挂载成功
还需要f_mount读取FatFS指令，做好初始化，这样才算挂载成功

通信(Communication)：也分为两个层面，一个是MCU与硬件外设之间的通信，一个是多个任务之间的数据传输和信息传递
硬件层面：通信可以分为三种关系
1.全双工(SPI)+同步:可以同时完成接发
2.半双工(I2C)+同步:只能同时完成接或发一个动作
3.异步(UART)+全双工:不依靠时钟源，靠特定波特率进行通信
软件层面：为了实现解耦和协同工作，通过队列，任务通知或者信号量进行数据传输

线与逻辑(Wire-AND Logic):多个设备输出引脚并联在同一根物理总线上，只要有一个设备输出0（低电平），总线就一直保持低电平，
当所有设备都输出1（高电平），总线才会被拉高（高阻态），在外部上拉电阻的作用下变为高电平
要求：所有设备必须是开漏输出，且有外部上拉电阻

# 12.I2C相关知识点
1.两线串行制：SCL(串行时钟线)和SDA(串行数据线)
2.挂载设备必须为开漏输出并外接上拉电阻
3.核心时序（高读低变）：SCL为高电平时，SDA电平不允许发生任何变化；SCL为低电平时，SDA才能改变电平
起始信号与停止信号：本质是对的占用和释放，当SCL为高电平时，SDA产生下降沿，为起始信号；SDA产生上升沿，为停止信号
4.高位读取：先读前7位bit，再读最后一位
高位读取的协议：SPI,I2C
低位读取的协议：UART
5.7位地址需要左移1位(Address << 1)，最后一位为0表示写操作，1表示读操作
6.半双工，只能同时完成接收或发送一个动作
7.应答机制：在第9个时钟周期，如果在高电平时期强行将SDA拉低，说明接收成功，为应答信号(ACK);如果没有拉低，说明接收失败，为非应答信号(NACK)

8.每次接收或发送一个Bit，都需要消耗一个时钟周期，SDA 在 SCL 低电平期间改变、高电平期间保持稳定
# 13.SPI相关知识点
1.四线串行制：SCK/MOSI(Master Out Slove In)/MISO(Master In Slove out)/CS
2.挂载设备选择推挽输出，并且每个设备都需要多接一条CS片选线，当主机需要唤醒从设备时，拉低片选
3.四种模式(CPHA (时钟相位)+CPOL(时钟极性))
CPHA = 0 CPHA = 1 （0为在第一个边沿读取，1为在第二个边沿读取） CPOL = 0 CPOL = 1 (决定SCL空闲时0为低电平，1为高电平)
常用Mode0 和 Mode3
Mode0：在上升沿期间采样锁存，在下降沿期间切换翻转
4.全双工，能够同时完成接收和发送动作，发送一个Bit,必须同时接收一个bit
5.读操作必须为0xFF(Dummy 哑数据)，从机没有内部时钟源，无法主动推送数据。这是为了强制把数据塞进去

# 14.I2C与SPI对比、使用场景
I2C:
1.只有2根线，引脚资源紧张的时候优先考虑
2.通信效率不高，性能较低
3.开销大，每次接发数据都需要额外的一个时钟周期作为应答
4.I2C靠地址寻址（一条总线多设备）
SPI:
1.有4根线，且每多一个外设都需要多一条CS线
2.通信效率极高，性能好
3.开销小
4.SPI靠CS片选

# 15.什么时候需要挂载总线？总线上能同时挂载多少个设备？低速和高速如何选择？
总线挂载分为硬件和软件两个层面，硬件层面本质是节省硬件资源，降低走线复杂度和实现模块化解耦
能挂载多少个设备取决于地址为多少位，例如I2C的7位寻址模式，则是2的七次方=128个，但是需要考虑寄生电容的问题
所以实际最多可能就挂载3~8个设备
低速总线：I2C/CAN（吞吐量小的）
高速总线：SPI/SDIO（数据吞吐量大的）

# 16.位运算法则(如何把特定位置1，置0，以及取反)
按位与&：有0则为0，全1才为1
按位或|：有1则为1，全0才为0
按位异或^：相同为0，相异为1
按位取反~:0变为1,1变为0

把第n位置1: data |= (1 << n)
把第n位置0：data &= ~(1 << n)
把第n位取反：data ^= (1 << n)

# 17.高低字节如何拼接、拆分，强制类型转换规则
两个8位拼成16位 0x12和0x34拼成0x1234
uint16_t u16_val = ((uint16_t)(0x12 << 8) | 0x34)
16位拆成两个8位 0x1234拆成0x12和0x34
uint8_t u1 = ((uint8_t)0x1234)  u1 = 0x34
uint8_t u2 = ((uint8_t)(0x1234 >> 8)) u2 = 0x12

强制类型转换规则：低转高，高位自动补0；高转低，默认保留最低8位
uint16 0x1234 转8位 uint8_t u8_val = (uint8_t)(0x1234)  u8_val = 0x34
0x12转16位 uint16_t u16_val = (uint16_t)(0x12) u16_val = 0x0012

# 18.结构体对齐规则、结构体和枚举命名规则、有无TypeDef区别
结构体对齐规则：
1.起始门牌号必须是自身字节大小的整数倍
2.整个结构体大小必须是最大字节的整数倍
3.一般按字节大小从大到小排列最省事最省空间

结构体和枚举命名规则：
{}前的是标签名，在结构体需要嵌套调用的时候需要加上，一般用于链表
{}后的是结构体/枚举的类型别名

有无TypeDef区别：
有TypeDef:类型别名可以直接定义变量
无TypeDef:需要struct ...不能直接定义变量

switch case中是否需要加括号：
case中如果重新定义了新的变量，就必须要加括号，{}代表的是一个大的作用域
如果没有定义新的变量，就不需要加括号

# 19.动态内存裸机是malloc,FreeRTOS里面是？，为什么不能滥用
FreeRTOS的动态内存是由configTOTAL_HEAP_SIZE决定，常用heap_4
为什么不能滥用动态内存：
1.容易产生内存碎片，没有足够的内存空间时会导致数据溢出，程序崩溃
2.每次申请和释放动态内存时间都具有不确定性，对时间高精度需求的任务使用容易导致时序错乱
3.每次申请内存都需要释放空间，如果忘记释放了就会导致空间一直积累，最后塞满空间

# 20.BSP/DEV/APP层的理解，如何理解高内聚、低耦合
BSP:只负责单片机与板载设备的对接逻辑，不会出现板外设备的引脚编号
DEV:向下负责调用BSP层接口函数，书写外接设备定义和逻辑，向上提供包装好的初始化函数
APP:向下调用底层函数，面对客户需求写入特定功能

高内聚：同一个模块内部的各元素紧密相关，所有代码都只为完成同一个明确的功能服务
低耦合：不同模块之间的相互依赖，关联程度尽量降到最低。任何一个模块发生变化不会引发其他模块崩溃

# 21.任务的本质和任务的四个状态，任务相关函数的应用
任务本质是一个不能return的while死循环函数，由独立私有栈和任务控制块（TCB）组成
四个状态：运行态、就绪态、阻塞态、挂起态
vTaskCreate:括号里的参数单位为字，1字等于4个字节
vTaskDelete:NULL为删除自身，但无法清除自己的任务控制块，需要交给优先级为0的空闲任务；其他参数为删除其他任务
vTaskSuspend/Resume:把任务挂起与返回就绪态
vTaskDelay/vTaskDelayUntil:一个是执行完程序后再延时一定时间，一个是限定整个程序执行时间为固定数字

# 22.任务和优先级考虑因素、裸机和FreeRTOS优先级规则
任务划分考虑因素：
1.按阻塞源划分：例如A要死等按键按下，B要死等串口数据发送，那就必须把A,B划分为两个任务
2.按耗时快慢划分：快任务和慢任务绝对不能放一起，否则快任务的逻辑会被覆盖掉造成程序崩溃，主要看有无延时函数
3.按共享资源度划分
4.按事件触发链的高内聚度合并
优先级划分考虑因素：
1.执行时间越短，对实时性响应要求越高的优先级要越高，因为没有足够高的优先级，会被其他高优先级延时任务阻塞信号，导致程序崩溃

裸机优先级数字越小，优先级越高
FreeRTOS优先级数字越大，优先级越高
裸机的硬件中断可以打断FreeRTOS任务，但是FreeRTOS任务不能打断中断
FreeRTOS任务（0-4）为超实时中断：不受RTOS临界区管理，严禁调用任何FreeRTOS API

# 23.stdio.stdint.,math等头文件什么时候要声明，<>和""的区别
stdio   有串口输出printf,snprintf就需要声明
stdint  有uint8_t,int16_t等命名整型变量就需要声明
math    有atan2f,sin,cos等数学运算就需要声明
string  有字符串sizeof,或者memcmp,memset就需要声明
<>  在标准库，整个系统里面找
""  在当前文件目录下找

# 24.旋转编码器原理，什么是数字滤波器
原理：左右旋转编码器时会产生脉冲信号，A相会产生边沿变化，当A相产生上升沿时，对B相电平进行判断。
B相为低电平时，说明A相超前B相90度，为顺时针（自增）；B相为高电平时，说明B相超前A相90度，为逆时针（自减）
4边沿倍频：产生一个脉冲信号，通常伴随4个边沿变化，所以在软件计算时通常需要除以4
硬件数字滤波器：根据filter值的大小决定滤波器强度
值为n，则需要在n个检测周期内电平都需要保持一致才能被确认为成功产生一个信号
0表示关闭，15表示最大

# 25.有源和无源蜂鸣器的区别
有源蜂鸣器内部有振荡源，但频率始终保持不变，只能发出b的声音
无源蜂鸣器内部没有振荡源，需要挂载PWM通道来改变频率，从而输出不同音符组成音乐

# 26.printf和snprintf的区别、以及为什么不能大量频繁使用，替代方案是什么？
printf是直接打印串口助手的数据，在keil5环境需要写fputc并勾选Lib,在cmake环境需要写_write
snprintf是直接把数据打包传进RAM内存数组内
printf打印速度取决于波特率，通常打印需要几毫秒，属于占用大量时间
如果大量频繁使用，会导致CPU卡死，时序错乱程序崩溃
替代方案：
1.严禁printf放在while(1),中断里面，改为事件驱动或定时器每隔1-2s才打印一次
2.用snprintf将数据丢入RAM数组里，配合FreeRTOS队列+DMA空闲中断实现0占用CPU打印数据

printf字符使用规则
1.精度
(1)float浮点数 .2lf表示保留两位小数（四舍五入） .0lf表示取整 %f默认保留6位小数
(2)%d整型数 %.4d表示保留四位数字，不足的自动在前面补零

2.对齐
%4d表示最小宽度为4（右对齐）    不足前面补空格
%-4d表示最小宽度为4（左对齐）   不足后面补空格
%04d表示最小宽度为4（右对齐）   不足前面补0 常见于时钟表达
%5.1f表示最小宽度为5，同时保留1位小数（右对齐） 常见于加速度和角姿态渲染

# 27.缓冲区类型、用途、使用场景、设置多大和选择类型的考虑因素
缓冲区分为普通线性缓冲区与环形缓冲区
线性缓冲区：静态数组+单个指针偏移
使用场景：snprintf格式化组装字符串，串口空闲中断

环形缓冲区：环形数组+Heap/Tail读写指针
使用场景：音乐播放，FreeRTOS任务消息队列

设置多大：有固定大小的，比如OLED屏幕规定需要1024字节，SD卡默认要512或4098字节
其他的根据2的n次方性能优化规则，同时考虑生产者与消费者速率的关系

# 28.环形缓冲区、环形数组、动态链表如何选择，写出核心代码逻辑
环形缓冲区：由环形数组构成的先进先出数据结构，核心为Head/Tail读写指针的应用
具有很高的实时性与精度，常用于串口DMA+IDLE,音频定时器节拍

动态链表：由离散的节点通过指针链接而成的数据结构，每个节点包含数据域与至少一个指针域
实时性不可控，适用于任务就绪列表/延时阻塞优先级排序队列

环形缓冲区代码主要分为：初始化，判空，判满，入队，出队
#include <stdint.h>
#include <stdbool.h>

#define RING_BUF_SIZE   128U
#define RING_BUF_MASK   (RING_BUF_SIZE - 1U) //掩码

typedef struct{
    uint8_t buffer[RING_BUF_SIZE];
    volatile uint16_t head;
    volatile uint16_t tail;
}ring_buffer_t;

初始化
void RingBuf_Init(ring_buffer_t *rb)
{
    rb->head = 0U;
    rb->tail = 0U;
}

判空
static inline bool RingBuf_IsEmpty(const ring_buffer_t *rb)
{
    return (rb->head == rb->tail);
}

判满
static inline bool RingBuf_IsFull(const ring_buffer_t *rb)
{
    return(((rb->tail) + 1U) & RING_BUF_MASK) == rb->head);
}

入队
bool RingBuf_Push(ring_buffer_t *rb,uint8_t data)
{
    uint16_t next_tail = (rb->tail + 1U) & RING_BUF_MASK;
    if(next_tail == rb->head)
    {
        return false;
    }
    rb->buffer[rb->tail] = data;
    rb->tail = next_tail;
    return true;
}

出队
bool RingBuf_Pop(ring_buffer_t *rb,uint8_t *p_data)
{
    if(RingBuf_IsEmpty(rb))
    {
        return false;
    }
    *p_data = rb->buffer[rb->head];
    rb->head = (rb->head + 1U) & RING_BUF_MASK;
    return true;
}

# 29.互斥锁类型、使用场景、注意事项
普通互斥锁：只能take和give一次，多次give会引发死锁导致程序卡死
递归互斥锁：可以多次take和give，需要相等次数才能恢复初始状态

使用场景：
1.外设硬件互斥：多任务争抢串口发送，OLED刷屏，读写SD卡内存
2.软件资源保护：并发读写状态机，配置结构体

优先级继承机制：拿到锁的低优先级任务，会暂时将优先级升高，在释放锁的一瞬间失效
所有权机制：谁拿锁，谁释放

注意事项：
互斥锁严禁在中断里使用，中断没有优先级，也没有任务控制块，互斥锁机制没办法发挥作用
port_MAX_DELAY严禁使用，需要设置合理的超时时长

# 30.__disable_irq临界区保护和互斥锁的区别和使用场景
临界段保护是从硬件层面切断中断线，任何情况都无法被打断
但是互斥锁可以被中断打断
临界段保护要求快进快出，不允许有延时函数，逻辑也要相对简单，通常为微秒级
互斥锁可以执行稍微复杂的逻辑，但一般不用延时函数，通常为毫秒级或者秒级
使用场景：有中断且需要保护的选择临界段，无中断的选择互斥锁

# 31.两种信号量的用法、注意事项
二值信号量：只有0和1，重复give不会累积计数
初始值默认为0，需要先take才能使用
使用场景：中断延迟处理，在中断执行到一半give一次，会立即退出中断，执行后台挂起的任务

二值信号量与互斥锁的区别：
二值信号量强调“同步”：事件发生了需要去处理，可以在中断使用，信号量可以Atake,Bgive
互斥锁强调“互斥”：共享资源独占保护，有优先级继承机制，但严禁在中断使用,互斥锁只能Atake,Agive

考虑因素：判断使用场景是需要保护（通常涉及优先级高低）还是单纯等待事件发生，以及是否在中断里使用

计数信号量：可以重复take和give，但需要相等次数才能恢复初始状态
使用场景：流量反压机制：考虑生产者和消费者速率的关系

# 32.什么是队列、传输类型、注意事项、队列集的用法
# 33.事件标志组和队列的对比
# 34.任务通知和信号量的对比
# 35.看门狗用法、使用场景、注意事项、核心函数
# 36.CRC32/魔数头/临时原子替换相关知识点
# 37.之前FreeRTOS项目都是单独建立任务、队列，但是现在整个项目都没有出现这种代码，这是什么原因？